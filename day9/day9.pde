size(640,360);
background(255);

int y = 80;//verticle location of each line
int spacing = 10;// how far apart is each line
int len = 20;// length of each line

//translation of the legs while loop to for loop.

.

  line(x,y,x,y + len);
  
}